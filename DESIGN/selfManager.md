命名：暂定
logo设计：待定  

设定：
1、每天打开一两次，用于回顾自我状态，所以不会用来做特别细致的任务管理追踪，不会做非常细节的提醒（如果要实现的话要把那个模块单独提出去做一个软件）；
2、

UI 规范补充：
- 目标模块的所有进度条统一使用系统蓝色（`systemBlue`），不随进度值动态变化；
- 保持进度条的圆角、大小和间距与现有风格一致；
- 进度百分比的数值显示也统一为蓝色，以增强一致性。